const authRouter = require("./auth");
const orgRouter = require("./org");
const inventoryRouter = require("./inventory");

module.exports = {
  authRouter,
  orgRouter,
  inventoryRouter,
};
